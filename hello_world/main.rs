fn main() {
    // calls a Rust macro (hence the !)
    // calling function would be without !
    println!("Hello, world!");
}
// rustc compiles program to .exe (executable file) and .pdb (debug file) on Windows